spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://${MYSQL_ADDRESS}/${MYSQL_DATABASE:kevin}
    username: ${MYSQL_USERNAME}
    password: ${MySQL_PASSWORD}
  jpa:
    database: mysql
    generate-ddl: true
    hibernate:
      ddl-auto: update
